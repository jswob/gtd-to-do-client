<TopMenu @label="Utwórz kolekcję">
    <LinkTo @route="user.collections.index" class="material-icons text-center py-1">
        arrow_back</LinkTo>
</TopMenu>

<CollectionForm @changeset={{changeset this.model.collection this.CollectionValidations}}
    @buckets={{this.model.buckets}} @buttonLabel="Utwórz kolekcję" />

{{outlet}}