<TopMenu @label="Edytuj kolekcję">
    <LinkTo data-test-arrow-back @route="user.collections.collection" class="material-icons text-center py-1">
        arrow_back</LinkTo>
</TopMenu>

<CollectionForm @changeset={{changeset this.model.collection this.CollectionValidations}}
    @buckets={{this.model.buckets}} @buttonLabel="Edytuj" />

{{outlet}}